# Known Issues

Running log of infra problems found, their root cause, and whether they're fixed. Update this whenever something new turns up or something on this list gets resolved.

## Open

### `downloads.kodyparton.com` routes to a dead host
- **Found:** 2026-08-17 (rediscovered 2026-08-24, still open)
- **What:** NPM's proxy host for qBittorrent forwards to `10.10.0.130:30024`, a host that doesn't respond to ping — same class of stale-migration issue as the n8n one below, just never fixed for this domain.
- **Actual location:** `192.168.178.69:30024` (this Mac, `network_mode: host`).
- **Fix (manual, NPM UI):** `192.168.178.69:81` → Hosts → Proxy Hosts → edit `downloads.kodyparton.com` → Forward Hostname/IP `192.168.178.69`, Forward Port `30024` → Save.
- **Why manual:** direct writes to NPM's live database/container have been blocked by a safety check as production-infrastructure changes that should go through NPM's own UI/API rather than a raw file/DB edit — see the NPM doc for why a DB row alone isn't even sufficient (routing is driven by generated nginx conf files, not just the database).

### `home.kodyparton.com` (Homepage internal URL) not yet live
- **Found:** 2026-08-24 (in progress)
- **What:** Two manual steps outstanding — a Cloudflare DNS record and an NPM proxy host. See `docs/containers/homepage.md` → External Access for exact values.
- **Why manual:** same reasoning as above; also, this one genuinely needs a real Cloudflare dashboard action from the account owner regardless of tooling.

### SMB mount passthrough — deeper fix deferred
- **Found:** 2026-08-24
- **What:** Root cause of the recurring Sonarr/Radarr/qBittorrent "lost connectivity" complaints — SMB shares mounted on the macOS host, passed through OrbStack's VM into containers, occasionally drop (`ENOENT`). Self-healing (workflow 09, restarts affected containers every 15min) is in place and covers the symptom.
- **Deeper fix (not done):** have containers mount the NAS via native Linux CIFS directly, bypassing the host-passthrough layer entirely — this is the actual fix, not just a workaround, but needs the NAS's SMB username/password (deliberately not extracted from macOS Keychain) and would touch how 5 running services access their data. Deferred at the user's choice on 2026-08-24 in favor of shipping the self-healer first.
- **Also affects (not yet covered by workflow 09):** LazyLibrarian (`/downloads`, `/books`) and Audiobookshelf (`/audiobooks`) use the same SMB shares but aren't in the self-healer's watch list — add them if either ever shows the same symptom.

### `heimdall/config/compose.yml` was empty — RESOLVED by removal
- No longer relevant — Heimdall was removed entirely 2026-08-17 in favor of Homepage (see below), since it was unconfigured and redundant.

## Resolved

| Date | Issue | Fix |
|---|---|---|
| 2026-08-17 | Live API keys + 4.5GB of runtime data about to be pushed to a **public** GitHub repo | Added `.gitignore`, moved secrets to gitignored `.env` files with committed `.env.example` templates |
| 2026-08-17 | n8n compose referenced undefined `${SUBDOMAIN}`/`${DOMAIN_NAME}`/`${GENERIC_TIMEZONE}`, broke `N8N_HOST` | Added `n8n/.env` |
| 2026-08-17 | n8n's NPM proxy host pointed at a dead host (`10.10.1.5:30065`, pre-migration leftover) | Fixed forward host/port in NPM UI |
| 2026-08-17 | `watchtower` crash-looping — image frozen at a 2023 build, incompatible Docker API version | Switched `containrrr/watchtower` → `nickfedor/watchtower` |
| 2026-08-17 | `lazylibrarian` unreachable — port mapped `30032:30032` but app listens on `5299` | Fixed to `30032:5299` |
| 2026-08-17 | `trilium` never started — bad `/etc/timezone`/`/etc/localtime` host bind mounts (incompatible with OrbStack), wrong data volume path | Removed the bind mounts (added `TZ` env var instead), pointed volume at `./data` |
| 2026-08-17 | `heimdall/config/compose.yml` was empty; service was unconfigured anyway | Removed entirely, replaced by Homepage |
| 2026-08-17 | `audiobookshelf` had never produced a backup — `backupSchedule: false` in settings, never enabled | Enabled daily 3am schedule |
| 2026-08-24 | n8n build agent hardcoded 6 real API keys directly into workflow node parameters instead of using n8n's credential store | Created proper `httpHeaderAuth`/`httpQueryAuth` credentials, rewired all affected nodes, verified zero plaintext keys remain |
| 2026-08-25 | Second brain: none of the 5 core LLM-calling nodes (Classify Intent, Generate Answer, Generate Chat Reply, Generate Media Answer, Match Task via LLM) had `continueOnFail` set. Under heavy concurrent load (self-inflicted, from running several live tests + a doc re-seed + an LLM tagging batch at once), `Generate Chat Reply` hit Ollama's own internal 3-minute request timeout and returned 500 — the entire webhook execution died with zero reply, worse than the normal slow-but-working case since it looks like total silence to the user | Added `continueOnFail: true` to all 5, plus fallback text ("I'm having trouble thinking right now...") on the 3 respond nodes that read their output directly. `Classify Intent` failing already degraded gracefully for free via `Parse Intent`'s existing try/catch |
| 2026-08-25 | Tagging backfill script: batching all 23 Discord-originated items into one LLM call to minimize round trips backfired — wall-clock time is dominated by *output* tokens on this hardware (~0.42 tokens/sec measured generation rate under load), not call count, so a ~700-900 output token response needed 25+ minutes and blew past every timeout tried (180s, 400s) | Switched to batches of 5 items per call, applying results incrementally so a later batch failing doesn't lose earlier progress. Also fixed a parsing bug where Ollama's `format:"json"` mode occasionally returned an object keyed by index instead of a bare array, crashing the parser on `.get()` against a string |
| 2026-08-25 | Vikunja connected for real for the first time (token had been a placeholder since it was built 2026-08-24) — surfaced two latent bugs immediately: `Create Vikunja Task` was missing `authentication`/`genericAuthType` parameters entirely, so the attached credential was never actually applied to the request (silently sent unauthenticated, indistinguishable from "no token" while the token was fake anyway); and `/api/v1/tasks/all` returns `400 Invalid model provided` on this Vikunja version (v2.5.0), breaking `list_tasks` and `complete_task`'s task fetch | Added the missing auth params to `Create Vikunja Task`; switched `Get Vikunja Tasks`/`Get Open Tasks` to the project-scoped `/api/v1/projects/1/tasks` endpoint (fine since there's only one real project, "Inbox"). Also found and fixed two more stale-`$json`-reference bugs from earlier same-day node insertions (the `Log Shadow (Complete Task)` write clobbering `$json` before `Mark Done Locally` read it, and `Write Cache` clobbering `$json` before `Embed Exchange` read `Generate Answer`'s response) — same root cause as the `forget` bug below, caught by live-testing each branch rather than trusting the structural review |
| 2026-08-25 | `tautulli/huntarr/compose.yml` was an exact duplicate of `huntarr/compose.yml`, nested in the wrong directory (pre-existing since 2026-08-17, not deployed as a second service, just confusing) | Removed `tautulli/huntarr/` entirely |
| 2026-08-25 | Host disk space critically low (down to 4.9GB free / 71% "used" on a 228GB disk — the mismatch is normal macOS APFS container accounting). Root cause had nothing to do with this Docker project: `~/Library/Caches/PlexMediaServer` (19GB, Plex's own image/transcode cache) and `~/Library/Containers/com.apple.mediaanalysisd` (6.4GB, Photos' ML analysis cache) — both pure, safe-to-clear regenerable caches for apps unrelated to this repo, running natively on the same Mac | Cleared both (with explicit user confirmation first, since neither is a service this project manages) plus routine `docker image prune`/`docker builder prune`/`brew cleanup`. Freed **~26GB total**, disk now at 31GB free / 28% used. Verified Plex still responds normally post-clear |
| 2026-08-25 | Second brain workflow 18 got reverted to an old pre-redesign version (missing Classify Intent and the whole intent-routing chain) — likely a stale n8n browser tab overwriting newer API-deployed changes on save | Redeployed the correct version. **Lesson**: after editing a workflow via the API, if you also have it open in a browser tab, refresh that tab before saving from it, or the stale copy wins |
| 2026-08-25 | Second brain: any question (not a stored fact, not chat, not a task) silently got zero reply — Ollama's classify step worked correctly, but the "question" branch relied on n8n Switch node's `fallbackOutput` mechanism, which never actually routed the item anywhere, so the item vanished with no error, and n8n's webhook returned an empty 200 | Replaced the fallback-based routing with an explicit `intent === "question"` condition (same proven pattern as the other 4 intents); the true fallback (any unexpected/malformed classify output) now safely routes to the chat branch instead of relying on `fallbackOutput` at all. Also bumped Ollama-calling node timeouts (60s→90-180s) and set `OLLAMA_KEEP_ALIVE=60m` since CPU-only inference in the container is genuinely slow (~35-40s for even a short classify call) — this was the original symptom reported ("failing on Classify Intent"), a real but separate issue from the routing bug |
| 2026-08-25 | `ollama` container was getting OOM-killed under load (`llama-server process has terminated: signal: killed` in its logs) — OrbStack's VM was capped at 8GB out of this Mac's 16GB total, not enough headroom for the 7B model plus everything else running concurrently | Increased OrbStack's VM memory allocation to 12GB (`orb config set memory_mib 12288`, requires `orb stop`/restart — all 23 containers came back up cleanly on their `restart:unless-stopped` policies) |
| 2026-08-25 | Second brain: `Parse Intent`'s intent whitelist never actually included `"task"` — every task-creation attempt was silently falling back to being treated as a `"question"` instead, since the addition of the `task` intent (2026-08-24) never updated this validation list | Added all 9 valid intents to the whitelist |
| 2026-08-25 | Second brain: the new `media_query` intent (checks live Sonarr/Radarr/Tautulli data) silently failed every time — n8n's HTTP Request node auto-splits a bare JSON array response into separate items rather than one array-valued item, which broke `.first().json.map(...)`-style code (throws "not a function", silently aborting the whole webhook execution with an empty response). The same bug existed in the new `list_tasks` and `complete_task` branches (Vikunja's task list is also a bare array) | Rewrote the affected code to reconstruct arrays via `$input.all().map(i => i.json)` / `$('NodeName').all().map(...)` instead of assuming a single array-valued item; for `media_query` specifically, inserted "Collect" nodes after each array-returning fetch to flatten split items back into one before the next HTTP call runs (otherwise each subsequent call would re-run once per split item, multiplying out) |
| 2026-08-25 | Second brain: `media_query` calls that did complete were dumping the *entire* Sonarr+Radarr library into the LLM prompt to answer even a simple yes/no question, taking 3+ minutes and hitting Ollama's own request timeout | Added client-side fuzzy word-matching (title vs. question) to filter to ~5 relevant library entries before ever calling the LLM; if literally nothing matches, skips the LLM call entirely and answers directly. An early version matched on raw substrings (e.g. "some" inside "Awesome"), producing false-positive matches — fixed to word-boundary matching |
| 2026-08-25 | Second brain: new question-caching layer (added to speed up repeated questions) silently returned an empty 200 response for *every* cache-miss message, not just repeats — the whole chat stopped working | Two stacked bugs: (1) the cache-check code read `$json.content` but webhook data actually lives under `$json.body.content` in n8n, so it was always hashing an empty string; (2) on a cache miss, the node feeding back into `Classify Intent` was carrying the *cache lookup's* HTTP response forward instead of the original message, so `Classify Intent`'s prompt expression (`$json.body.content`) crashed on `undefined` — a synchronous expression error, not a network failure, so it wasn't caught by `continueOnFail` and returned instantly with no body. Found via the same "tap a `respondToWebhook` node in right after each new node" bisection technique used for the earlier fallback-routing bug; fixed by correcting the field path and adding an explicit "Restore Original Data" node that re-reads the original message by name before handing off to `Classify Intent`. **Lesson**: any node inserted between the webhook trigger and a downstream node that reads `$json.body.*` must either preserve that shape or restore it explicitly — an HTTP node in between will silently replace it |
| 2026-08-25 | Second brain: after adding a shadow/undo log ahead of `forget`'s delete step, `forget` stopped actually deleting anything (confirmed a "removed" confirmation was sent but the fact was still in Qdrant) | The delete step's request body referenced `$json.result` expecting the *search* node's output, but after inserting the shadow-log write node in between, `$json` was now the shadow-log write's response instead — same class of bug as the array-splitting issue from 2026-08-24, just via node insertion instead of an HTTP auto-split. Fixed by referencing `$('Search for Match').first().json.result` by name instead of the ambient `$json`. Caught by live-testing store-then-forget on a throwaway marker fact and directly checking Qdrant for it afterward, rather than trusting the confirmation message alone |
| 2026-08-25 | Investigated whether this hardware could run vision-capable photo understanding (a 3rd concurrently-loaded model, alongside the 7B classify/generate model and the embedding model) | Pulled `moondream` (1B params, deliberately tiny) and tested it directly against Ollama with a real image. Image encoding itself was fine (~7s), but text generation ran at **0.49 tokens/sec** — a ~124-token description didn't finish within a 4:40 timeout. Root cause: severe CPU contention from 3 simultaneously-loaded `llama-server` processes competing for the same ~8 cores, not the vision model's own size. Confirms the standing "untested" caution in `second-brain.md`'s ideas list was warranted — abandoned, model removed. Would need either better hardware or Ollama unloading the 7B model while photos are being processed (which would just slow down concurrent text messages instead) |
| 2026-08-25 | Investigated switching `Classify Intent` to a smaller/faster model (`qwen2.5:1.5b-instruct`, 5x fewer params than the 7B model in use) to speed up the dominant per-message latency cost | Pulled and load-tested it directly against Ollama — two separate calls both took ~50s, statistically the same as the 7B model, while pegging ~6 CPU cores the whole time. On this Mac's CPU-only inference, single-request latency is dominated by prefill/JSON-grammar-decode overhead and memory bandwidth, not raw parameter count, so a smaller model doesn't give a proportional speedup. Model removed (`ollama rm qwen2.5:1.5b-instruct`) to reclaim disk space; this optimization is a dead end on current hardware unless a fundamentally lighter-weight model is tried. While investigating, measured a real end-to-end "chat" round trip (classify + generate, both against the 7B model) at **2:43** under light concurrent load — well past the "~30 seconds" the user guide previously claimed. Corrected `docs/second-brain-guide.md` to set realistic expectations (30s–a few minutes) and added `docs/architecture/troubleshooting-second-brain.md` |
| 2026-08-26 | Huntarr had been running since Jan 2026 with zero real Sonarr/Radarr connection — empty `api_key`/`api_url` on both, Radarr's address was `http://localhost:7878` (unreachable from inside the container). `GET /api/stats` confirmed `hunted: 0, upgraded: 0` for every app, its entire runtime — never once found or upgraded anything despite running 24/7 | Found via a full stack review, not a user report. Fixed via `POST /api/settings/<app>` (the actual save endpoint — `/api/settings` itself is read-only, 405s on write) with real API keys from Infisical, and added the missing Sonarr 4K / Radarr 4K instances (only the standard tier had ever been attempted). Verified immediately: found 428 real missing episodes, triggered a real season search, `hunted` count moved 0→3 within a minute. Also debunked a red herring: `Loaded schedules: {'global': 0, ...}` in its logs looks like "hunting disabled" but isn't — hunting worked fine with that still at 0, it's an unrelated feature. See `docs/architecture/stack-review-2026-08-26.md` |
| 2026-08-26 | Unpackerr had been failing to extract anything since setup — every completed download failed with "no such file or directory", retried forever | Its mount (`~/mnt/downloads`) was never an alias for the real downloads share — a completely different, empty local directory. Sonarr/Radarr actually place files at `/Volumes/downloads`. Also silently accumulated 55MB of Unpackerr's own log files in that same wrong directory (`UN_LOG_FILE` pointed there too). Fixed the mount to `/Volumes/downloads:/mnt/downloads`, removed `UN_LOG_FILE` in favor of plain `docker logs`, deleted the stale logs. Verified live — errors stopped immediately, previously-stuck items were found correctly on restart. See `docs/architecture/stack-review-2026-08-26.md` |
